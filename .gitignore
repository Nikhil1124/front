node_modules/
.expo/
dist/
build/
*.log
.DS_Store
.env*.local

# Continuous Native Generation: android/ and ios/ are generated by
# `expo prebuild` / `expo run:*` from app.json + installed config plugins.
# Do not hand-edit or commit them — regenerate with `npx expo prebuild --clean`.
android/
ios/
*.keystore
*.jks
