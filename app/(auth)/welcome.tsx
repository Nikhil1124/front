import { SignInScreen } from '@/features/auth/SignInScreen';

// The app's entry point when there is no session. Used to be a portal picker asking which of
// four roles you were — a question `POST /auth/login` answers on its own from the credentials.
export default SignInScreen;
